# AI数据模式分析：Databricks、Palantir 与 Agent

> 基于AI在行业落地场景，对三种典型数据模式的对比研究

---

## 一、AI行业落地的数据本质

AI在行业落地，本质是**"数据—模型—决策—行动"**的闭环。不同的数据组织方式决定了AI能解决什么样的行业问题：

- **数据是否结构化** → 决定模型能否直接消费
- **数据是否实时** → 决定AI能否参与实时决策
- **数据是否带语义/本体** → 决定AI能否理解业务
- **数据是否可被Agent调用** → 决定AI能否自主行动

围绕这四个维度，业界形成了三种代表性的数据模式：**Databricks（湖仓一体）**、**Palantir（本体驱动）**、**Agent（任务/记忆驱动）**。

---

## 二、Databricks 的数据模式：湖仓一体 + AI数据平台

### 2.1 核心架构

**Lakehouse（湖仓一体）= Data Lake（湖）+ Data Warehouse（仓）**

| 层次 | 组件 | 作用 |
|------|------|------|
| 存储层 | Delta Lake (Parquet + 事务日志) | 统一存储原始/加工数据，支持ACID |
| 元数据层 | Unity Catalog | 跨工作区的统一权限、血缘、发现 |
| 计算层 | Spark / Photon / Serverless SQL | 批流一体计算 |
| 智能层 | Mosaic AI / DBRX / Vector Search | 模型训练、RAG、特征工程 |
| 应用层 | Genie / AI Agent / Apps | 自然语言取数、智能体应用 |

### 2.2 数据模式特征

- **数据中心化**：所有结构化、半结构化、非结构化数据汇入Delta Lake，"一份数据多种用途"（BI / ML / GenAI）
- **Schema-on-Read**：先存后建模，灵活适配新业务
- **Medallion架构**：Bronze（原始）→ Silver（清洗）→ Gold（业务聚合）三层逐级精炼
- **数据即代码**：通过Notebook + Workflow + DLT实现数据管道版本化
- **向量与表共存**：Vector Search直接挂在Delta表上，结构化数据与Embedding共生

### 2.3 行业落地场景

| 行业 | 典型场景 | 数据模式价值 |
|------|---------|-------------|
| 金融 | 反欺诈、风险评分、合规报告 | 海量交易流水汇入湖仓，批流一体训练风控模型 |
| 零售 | 个性化推荐、需求预测 | POS+电商+CRM统一在Lakehouse，特征复用 |
| 医疗 | 临床数据分析、医学影像+病历联合建模 | 多模态数据共存于Delta |
| 制造 | 设备预测性维护 | IoT时序数据 + ERP结构化数据联合分析 |

### 2.4 优势与局限

**优势**：
- 工程师友好，技术栈开放（Spark/Python/SQL）
- 通用性强，几乎可承载任何数据负载
- 与开源生态深度融合（MLflow、Iceberg、Hugging Face）

**局限**：
- "数据平台"而非"业务平台"——业务语义需要客户自己沉淀
- 对业务专家不友好，需要数据工程师做中介
- 决策闭环（从数据到行动）需自行搭建

---

## 三、Palantir 的数据模式：Ontology（本体）驱动

### 3.1 核心架构

**Ontology = Objects（对象）+ Properties（属性）+ Links（关系）+ Actions（动作）**

| 层次 | 组件 | 作用 |
|------|------|------|
| 数据接入 | Foundry Pipeline Builder | 从SAP/Oracle/IoT/文档接入 |
| 本体层 | **Ontology** | 把数据抽象成业务对象（订单、设备、患者、士兵...） |
| 决策层 | AIP (Artificial Intelligence Platform) | LLM 在 Ontology 上做推理和决策 |
| 行动层 | Actions / Workshop / Operator | 把AI决策反向写回业务系统（ERP/MES/指挥系统） |

### 3.2 数据模式特征

- **业务语义优先**：数据进入系统的第一步是被映射成"业务对象"，而非"表"
- **数字孪生**：Ontology是企业业务的实时数字镜像（订单、设备、人员的虚拟副本）
- **可写回（Write-back）**：决策可以从Ontology直接触发现实世界动作（下单、调度、发指令）
- **权限粒度极细**：每个对象、每个字段、每个动作都可独立授权（军政场景必需）
- **AI在本体之上推理**：LLM不是直接读数据库，而是读"业务模型"，幻觉显著降低

### 3.3 行业落地场景

| 行业 | 典型场景 | Ontology的价值 |
|------|---------|---------------|
| 国防/情报 | 战场态势、目标识别、指挥决策 | 把雷达、卫星、人员、武器全部建模为对象，AI做实时决策 |
| 医疗 | 医院运营、临床试验 | 患者、床位、药物、医生作为对象互相关联 |
| 制造 | 供应链优化（空客、宝马） | 工厂、产线、零件、订单全本体化，AI做调度 |
| 能源 | 油田生产优化 | 井、管道、设备、产量统一建模 |
| 政府 | 公共卫生、社会治理 | 跨部门数据通过本体打通 |

### 3.4 优势与局限

**优势**：
- **从数据到决策到行动**的完整闭环
- 业务专家可直接使用，无需数据工程师
- AI幻觉低（因为有Ontology约束）
- 适合"高风险、高合规、强决策"场景

**局限**：
- 重交付、重咨询，标准化产品成熟度待提升
- 价格高，中小客户难以承受
- 本体建模需要深度业务参与，启动周期长
- 生态相对封闭

---

## 四、Agent 的数据模式：任务 + 记忆 + 工具调用

### 4.1 核心架构

**Agent数据 = Context（上下文）+ Memory（记忆）+ Tools（工具）+ Trace（轨迹）**

| 层次 | 组件 | 作用 |
|------|------|------|
| 上下文层 | Prompt + RAG + Vector DB | 把任务相关数据动态注入LLM |
| 记忆层 | Short-term (会话) + Long-term (向量/图记忆) | Agent跨任务、跨时间的状态 |
| 工具层 | Tool Calling / MCP / API Schema | Agent调用外部系统的"动作接口" |
| 编排层 | LangGraph / AutoGen / CrewAI / Claude Agent SDK | 多Agent协作与任务分解 |
| 评测层 | Trace / Eval / Replay | Agent行为日志、回放、优化 |

### 4.2 数据模式特征

- **数据围绕"任务"组织**：不再是"先建仓再用"，而是"任务发起 → 动态拉取 → 用完归档"
- **非结构化优先**：文档、对话、网页、代码、截图……Agent的输入大多是非结构化
- **记忆即数据资产**：Agent的长期记忆（用户偏好、历史决策、教训）成为新的"数据沉淀"
- **工具Schema即接口数据**：MCP/Tool Schema定义了Agent能"看见"和"操作"的世界
- **轨迹数据反哺训练**：Agent的执行Trace成为RL/SFT的高价值训练数据

### 4.3 行业落地场景

| 行业 | 典型Agent | 数据模式 |
|------|----------|---------|
| 软件开发 | Claude Code / Cursor / Devin | 代码库 + 文件树 + 终端 + Git历史作为上下文 |
| 客服 | Sierra / Decagon | 工单系统 + 知识库 + CRM作为工具，对话作为记忆 |
| 销售/营销 | 11x / Clay | CRM + 邮件 + LinkedIn + 网页作为上下文 |
| 金融 | Hebbia / Rogo | 财报、研报、合同作为RAG数据源 |
| 法律 | Harvey | 判例、合同、法规作为长上下文 |
| 医疗 | Abridge / Ambience | 医患对话 → 病历结构化 |

### 4.4 优势与局限

**优势**：
- 启动快、零代码（自然语言编排）
- 直接消费非结构化数据，规避ETL
- "用即建模"，不需要预先架构
- 闭环到行动（通过工具调用）

**局限**：
- 缺乏统一数据治理（每个Agent都在"造数据小仓库"）
- 记忆/上下文的可观测性、合规性挑战大
- 多Agent间数据共享标准未成熟（MCP仍在演进）
- 长期可靠性、幻觉控制仍是挑战

---

## 五、三种模式的对比

| 维度 | Databricks | Palantir | Agent |
|------|-----------|----------|-------|
| **核心抽象** | 表/Delta文件 | 业务对象（Ontology） | 任务/上下文 |
| **数据组织** | Schema-on-Read，湖仓 | 本体+图+权限 | 动态拉取+记忆 |
| **首要用户** | 数据工程师/科学家 | 业务专家/决策者 | 终端用户/操作员 |
| **AI接入方式** | 模型在数据上训练/推理 | LLM在Ontology上推理 | LLM编排工具与记忆 |
| **决策闭环** | 弱（需自建） | 强（Action写回） | 强（Tool Calling） |
| **结构化要求** | 中—高 | 高 | 低（拥抱非结构化） |
| **启动成本** | 中 | 高 | 低 |
| **适合行业** | 金融、零售、互联网、制造 | 国防、能源、医疗、政府 | 软件、客服、销售、专业服务 |
| **典型价值** | 降低数据工程成本 | 业务决策的数字化 | 替代/增强人的工作流 |
| **数据治理成熟度** | 高（Unity Catalog） | 极高（细粒度权限） | 早期（治理标准未定） |

---

## 六、三种模式的关系：互补而非替代

**这三种模式在AI产业的"倒金字塔"中各司其职：**

```
         ┌──────────────────────────────────┐
         │     行业应用 / Agent (100x)      │ ← 直接交付业务价值
         │   任务、记忆、工具调用、闭环     │
         ├──────────────────────────────────┤
         │      业务语义层 / Palantir       │ ← 把数据翻译成业务
         │      Ontology + AIP + Action     │
         ├──────────────────────────────────┤
         │      数据底座 / Databricks       │ ← 统一数据基础设施
         │   Lakehouse + Unity + Mosaic AI  │
         ├──────────────────────────────────┤
         │             硬件 (1x)             │
         └──────────────────────────────────┘
```

**典型组合落地路径**：

1. **数据汇聚** → Databricks 把企业全域数据汇入Lakehouse
2. **业务建模** → Palantir-style Ontology 在Lakehouse之上抽象业务对象
3. **智能行动** → Agent 基于Ontology + 工具调用执行任务
4. **反馈沉淀** → Agent的Trace + 决策结果回写到Lakehouse，形成数据飞轮

---

## 七、对行业智能化项目成功的启示

1. **不要混淆"数据平台"与"业务平台"**：Databricks解决"数据可用"，Palantir解决"业务可用"，Agent解决"任务可用"。先想清楚要解决哪一层问题。

2. **本体（Ontology）是行业智能化的关键中间层**：没有业务语义，AI只能做表面文章。无论用Palantir还是自建，Ontology层不可省略。

3. **Agent是价值放大器，但需要稳固数据底座**：Agent能直接交付业务价值，但其可靠性高度依赖底层数据和本体的质量。"垃圾进，垃圾出"在Agent时代依然成立。

4. **数据飞轮的新形态**：Agent的执行轨迹（Trace）、决策记忆是新型数据资产，要从一开始就规划这些数据的回流和复用。

5. **行业智能化的"倒金字塔"路径**：
   - 短期（1–2年）：在Databricks类湖仓上做RAG、做特征工程
   - 中期（2–3年）：构建行业Ontology，把数据翻译成业务对象
   - 长期（3–5年）：基于Ontology部署Agent群，实现"决策—行动"闭环，行业应用价值规模化释放

---

*文档编写时间：2026-04-26*
